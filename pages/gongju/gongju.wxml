<view class="page {{isIphoneX?'iphone-x':''}}">
    <view class="jiangqie-timeline-head">
        <image mode="aspectFill" src="https://vkceyugu.cdn.bspapp.com/VKCEYUGU-f7f3f46c-2ca3-4514-81d9-6144e0e5180f/3a630d7b-8497-4949-8402-8155e5ef4db4.jpg"></image>
        <view class="timeline-head-text">
            <text>工具宝箱</text>
            <view class="timeline-head-describe">林深时见鹿，海蓝时见鲸，梦醒时见你。</view>
        </view>
    </view>
    <view class="tab-content">
        <view class="index">
            <view class="listtit">
                <image class="jiangqie-news-iconimg" src="https://sculpture.xmamx.com/wp-content/uploads/2022/02/CD.png"></image>
                <text class="listleft">精品推荐</text>
            </view>
            <ad class="bgads" unitId="adunit-056807f43659baad"></ad>
        </view>
    </view>
    <view class="tab-content">
        <view class="index">
            <view class="listtit">
                <image class="jiangqie-news-iconimg" src="https://sculpture.xmamx.com/wp-content/uploads/2022/02/CD.png"></image>
                <text class="listleft">黑科技工具集合</text>
            </view>
            <view class="ToolAll">
                <view class="ToolLeft">
                    <view bindtap="gotoDetail" class="one" data-url="{{item.url}}" wx:if="{{index%2!==1}}" wx:for="{{toolsall1}}">
                        <image class="toolicon" mode="widthFix" src="{{item.src}}"></image>
                        <text class="onetitle">{{item.title}}</text>
                        <text class="onedes">{{item.des}}</text>
                    </view>
                </view>
                <view class="ToolRight">
                    <view bindtap="gotoDetail" class="one" data-url="{{item.url}}" wx:if="{{index%2==1}}" wx:for="{{toolsall1}}">
                        <image class="toolicon" mode="widthFix" src="{{item.src}}"></image>
                        <text class="onetitle">{{item.title}}</text>
                        <text class="onedes">{{item.des}}</text>
                    </view>
                </view>
            </view>
            <tui-loading visible="{{loading}}"></tui-loading>
            <view class="last_text" wx:if="{{isLastPage&&posts.length>0}}">已经到底啦~</view>
            <view class="last_text" wx:if="{{!isLastPage&&posts.length>0}}">努力加载中...</view>
        </view>
    </view>
</view>
